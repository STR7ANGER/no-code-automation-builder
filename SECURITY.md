# Security policy

Report vulnerabilities privately through GitHub Security Advisories. Do not include credentials, customer payloads, or exploit traffic in public issues.

Supported code is the latest commit on `main`. Rotate any potentially exposed API key, connector credential, encryption key, bootstrap key, session pepper, or operator token before reporting. Include affected route, tenant-isolation impact, reproduction steps using synthetic data, and the earliest known occurrence.
